<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/ap_rood</item>
        <item name="colorPrimaryDark">@color/ap_donkerrood</item>
        <item name="colorAccent">@color/ap_grijs</item>
        <item name="android:textColorPrimaryInverse">@color/wit</item>
        <item name="android:textColorPrimary">@color/wit</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ap_thema" parent="AppTheme" >
        <item name="android:textColorPrimary">@color/wit</item>
    </style>

    <style name="ap_input">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginEnd">8dp</item>
        <item name="android:layout_marginStart">8dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:ems">10</item>
        <item name="android:inputType">textPersonName</item>
        <item name="android:textColor">@color/common_google_signin_btn_text_dark_focused</item>
    </style>

</resources>
